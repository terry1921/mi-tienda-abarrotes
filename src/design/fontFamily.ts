const fontFamily = {
    // Define custom font families
    header: ['Roboto Slab', 'serif'], // Usada para encabezados
    body: ['Open Sans', 'sans-serif'], // Usada para el texto del cuerpo
}

export default fontFamily;